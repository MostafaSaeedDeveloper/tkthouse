@extends('front.layout.master')
@section('content')<h2>{{ $event->title }}</h2><form method="POST" action="{{ route('front.checkout.store') }}">@csrf @foreach($event->ticketTypes as $type)<div>{{ $type->name }} - {{ $type->price }}<input type="number" name="items[{{ $loop->index }}][qty]" value="0"><input type="hidden" name="items[{{ $loop->index }}][ticket_type_id]" value="{{ $type->id }}"></div>@endforeach<button>Checkout</button></form>@endsection
